# Doyeon Kim — Portfolio

Interactive portfolio dashboard built with Streamlit.

## Local run
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy
Hosted on Streamlit Community Cloud. Point the service at `app.py` on the default branch.
